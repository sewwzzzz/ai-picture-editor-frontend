# 功能分解

## Vite + React + TS

### 声明范围（`package.json`）

```
vite                             ^7
@vitejs/plugin-react             ^4.3
react / react-dom                ^19.2
typescript                       ^5.9
@types/react / @types/react-dom  ^19
@types/node                      ^26   # 路径别名所需
```

### 实际安装版本（2026-09-20，`npm list --depth=0`）

| 依赖 | 声明 | 实际安装 |
| --- | --- | --- |
| `vite` | ^7 | **7.3.6** |
| `@vitejs/plugin-react` | ^4.3 | **4.7.0** |
| `react` / `react-dom` | ^19.2 | **19.3.0** |
| `typescript` | ^5.9 | **5.9.3** |
| `@types/react` / `@types/react-dom` | ^19 | **19.3.0** |
| `@types/node` | ^26 | **26.6.2** |

> 全部落在声明范围内；`react` 与 `react-dom` 同版本，`@types/*` 与 React 大版本对齐（19）。
> 依赖**精确版本**以 `package-lock.json` 为准。

### 路径别名 `@/*` → `src/*`

**必须两处成对配置**，只配一边会导致 TS 与 Vite 行为不一致：

| 解析器 | 配置文件 | 内容 |
| --- | --- | --- |
| TypeScript（编辑器提示、`tsc`） | `tsconfig.app.json` | `"baseUrl": "."`、`"paths": { "@/*": ["./src/*"] }` |
| Vite（`dev` / `build` 打包） | `vite.config.ts` | `resolve.alias` + `fileURLToPath(new URL('./src', import.meta.url))` |

`@types/node` 是 `vite.config.ts` 里 `import ... from 'node:url'` 的类型依赖，缺它 `tsc -b` 会报 `Cannot find module 'node:url'`。

> 冒烟已通过：`npm run typecheck` ✓、`npm run build` ✓（Vite 7.3.6，28 modules）。

## 规划目录结构

1. 详见[目录结构](../rules/directory-structure.md)（唯一真源）

